import React, { Component } from "react"

import styles from "./Header.less"

class Header extends Component {
  render() {
    return <div className={styles.header}>头部</div>
  }
}

export default Header
